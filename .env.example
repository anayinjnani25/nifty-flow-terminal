MARKET_OPTION_CHAIN_PROVIDER=mock

# Switch to `nse-public` only if you accept the limitations of public web endpoints.
# For production, replace the provider with a broker or licensed market-data feed.

